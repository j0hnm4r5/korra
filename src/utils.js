import chalk from 'chalk';

// Useful constants
export const { log } = console;
export const k = chalk.bold.cyan('korra');

export const Commands = {
  format: 'format',
  lint: 'lint'
};

export const Aliases = {
  f: 'format',
  lint: 'lint'
};
